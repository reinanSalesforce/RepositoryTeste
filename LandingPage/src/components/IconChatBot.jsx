/*import React, { useState } from "react";
import Chat from "./components/ChatTeste"; // Certifique-se de que o nome do componente está correto
import { FaComments } from "react-icons/fa"; // Ícone de chatbot (requer `react-icons`)

function IconChatbot() {
  const [showChat, setShowChat] = useState(false);

  const toggleChat = () => setShowChat(!showChat);

  return (
    <>
      {/* Outros componentes }
      <Footer />

      {/* Ícone de Chatbot }
      <div className="chat-icon" onClick={toggleChat}>
        <FaComments size={30} />
      </div>

      {/* Chatbot em modal/popup }
      {showChat && (
        <div className="chat-popup">
          <Chat />
        </div>
      )}
    </>
  );
}

export default IconChatbot;*/
