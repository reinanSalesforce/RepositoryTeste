image: node:20.18.0

clone:
  depth: full
pipelines:
  default:
    - step:
        script:
         # Configurar o usuário do Git
          - git config --global user.email "reinanappa0@gmail.com"
          - git config --global user.name "reinanSalesforce"
           # Push todas as branches, exceto as que são bloqueadas
          - git push origin --all
          - git push origin --tags